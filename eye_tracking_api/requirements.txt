# ---- Core Framework ----
fastapi==0.115.5
uvicorn[standard]==0.30.6
python-multipart==0.0.9

# ---- ML / CV Stack ----
numpy==2.1.3
scipy==1.13.1
scikit-learn==1.7.1
joblib==1.4.2
scikit-image==0.24.0
opencv-python-headless==4.10.0.84

# ---- Optional: for local GUI testing (remove headless above if you use this)
# opencv-python==4.10.0.84

# ---- Notes ----
# - These versions are verified to work on macOS (Apple Silicon, ARM64)
# - Compatible with Python 3.11.x
# - Matches your model trained on scikit-learn 1.7.1
# - If running on server (no display), keep `opencv-python-headless`
# - If running locally and need imshow() windows, swap to `opencv-python`

# Run on new System 
# conda create -n eye311 python=3.11 -y
# conda activate eye311
# python -m pip install -U pip
# pip install -r requirements.txt
# uvicorn main:app --reload --host 0.0.0.0 --port 8000